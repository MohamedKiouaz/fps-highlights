[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fps-highlights"
version = "0.1.0"
dependencies = [
    "numpy>=2.3.2",
    "moviepy>=2.2.1",
    "fastai>=2.8.4",
    "loguru>=0.7.3",
    "tqdm>=4.67.1",
]
